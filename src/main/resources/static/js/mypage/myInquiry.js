'use strict';

document.addEventListener('DOMContentLoaded', () => {
    const detailLinks = document.querySelectorAll('.ev-inquiry-card .btn');

    detailLinks.forEach((link) => {
        link.addEventListener('click', () => {
        });
    });
});